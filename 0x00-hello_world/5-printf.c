#include <stdio.h>
/**
 * main - prints out a string with printf
 * Return: Always 0(success)
 * Description: uses the main function to print out a string
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
