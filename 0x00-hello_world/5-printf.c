#include <stdio.h>
/**
 * main - prints out string with printf
 * Return: Always 0(success)
 * Description: uses the main function to print a string
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n") 
		return (0)
}
