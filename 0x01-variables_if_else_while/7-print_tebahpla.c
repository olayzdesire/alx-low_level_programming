#include <stdio.h>
/**
 * main -  prints all alphabets in reverse 
 * Description: using putchar
 * Return: 0
 */
int main(void)
{
	char ch0;

	for (ch0 = 'z'; ch0 >= 'a'; ch0--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
