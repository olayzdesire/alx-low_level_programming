#include <stdio.h>

/**
 * main - prints alphabets in lower case
 * Description: using putchar
 * Return: 0
 */
int main(void)
{
	char char0

	for (ch0 = 'a'; ch0 <= 'z'; ch0++)
	{
		putchar(ch0);
	}
	putchar('\n');
	return (0);
}
